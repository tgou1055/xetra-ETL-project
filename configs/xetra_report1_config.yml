# Logging Configuration
logging:
  version: 1
  formatters:
    xetra:
      format: "Xetra Transformation - %(asctime)s - %(levelname)s - %(message)s"
  handlers:
    console:
      class: logging.StreamHandler
      formatter: xetra
      level: DEBUG
  root:
    level: DEBUG
    handlers: [console]
